#pragma once

#include "aare/InclusiveROI.hpp" // IMPORTANT: Uses InclusiveROI!!!
#include "aare/RemapConfig.hpp"

#include <optional>

namespace aare::remap::config {

class StrixelSensorConfig
{
private:
    /* data */
public:
    StrixelSensorConfig(/* args */);
    // ~StrixelSensorConfig();
};

StrixelSensorConfig::StrixelSensorConfig(/* args */)
{
}

// StrixelSensorConfig::~StrixelSensorConfig()
// {
// }


}